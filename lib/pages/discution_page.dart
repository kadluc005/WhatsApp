import 'package:flutter/material.dart';

class DiscutionPage extends StatelessWidget {
  const DiscutionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Discussions"),
    );
  }
}
